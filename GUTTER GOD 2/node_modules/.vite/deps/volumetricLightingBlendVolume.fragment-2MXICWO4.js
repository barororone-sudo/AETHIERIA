import {
  ShaderStore,
  init_shaderStore
} from "./chunk-THV6IJPM.js";
import {
  __esm
} from "./chunk-V4OQ3NZ2.js";

// ../../../node_modules/@babylonjs/core/Shaders/volumetricLightingBlendVolume.fragment.js
var name, shader, volumetricLightingBlendVolumePixelShader;
var init_volumetricLightingBlendVolume_fragment = __esm({
  "../../../node_modules/@babylonjs/core/Shaders/volumetricLightingBlendVolume.fragment.js"() {
    init_shaderStore();
    name = "volumetricLightingBlendVolumePixelShader";
    shader = `varying vec2 vUV;uniform sampler2D textureSampler;uniform sampler2D depthSampler;uniform mat4 invProjection;uniform vec2 outputTextureSize;
#ifdef USE_EXTINCTION
uniform vec3 extinction;
#endif
#define CUSTOM_FRAGMENT_DEFINITIONS
void main(void) {gl_FragColor=texture2D(textureSampler,vUV);
#ifdef USE_EXTINCTION
float depth=texelFetch(depthSampler,ivec2(gl_FragCoord.xy),0).r;vec4 ndc=vec4((gl_FragCoord.xy/outputTextureSize)*2.-1.,depth*2.-1.,1.0);vec4 viewPos=invProjection*ndc;viewPos=viewPos/viewPos.w;float eyeDist=length(viewPos);gl_FragColor2=vec4(exp(-extinction*eyeDist),1.0);
#endif
}
`;
    if (!ShaderStore.ShadersStore[name]) {
      ShaderStore.ShadersStore[name] = shader;
    }
    volumetricLightingBlendVolumePixelShader = { name, shader };
  }
});
init_volumetricLightingBlendVolume_fragment();
export {
  volumetricLightingBlendVolumePixelShader
};
//# sourceMappingURL=volumetricLightingBlendVolume.fragment-2MXICWO4.js.map
