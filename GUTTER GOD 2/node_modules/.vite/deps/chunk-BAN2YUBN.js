import {
  ShaderStore,
  init_shaderStore
} from "./chunk-THV6IJPM.js";
import {
  __esm
} from "./chunk-V4OQ3NZ2.js";

// ../../../node_modules/@babylonjs/core/Shaders/bloomMerge.fragment.js
var name, shader, bloomMergePixelShader;
var init_bloomMerge_fragment = __esm({
  "../../../node_modules/@babylonjs/core/Shaders/bloomMerge.fragment.js"() {
    init_shaderStore();
    name = "bloomMergePixelShader";
    shader = `uniform sampler2D textureSampler;uniform sampler2D bloomBlur;varying vec2 vUV;uniform float bloomWeight;
#define CUSTOM_FRAGMENT_DEFINITIONS
void main(void)
{gl_FragColor=texture2D(textureSampler,vUV);vec3 blurred=texture2D(bloomBlur,vUV).rgb;gl_FragColor.rgb=gl_FragColor.rgb+(blurred.rgb*bloomWeight); }
`;
    if (!ShaderStore.ShadersStore[name]) {
      ShaderStore.ShadersStore[name] = shader;
    }
    bloomMergePixelShader = { name, shader };
  }
});

export {
  bloomMergePixelShader,
  init_bloomMerge_fragment
};
//# sourceMappingURL=chunk-BAN2YUBN.js.map
