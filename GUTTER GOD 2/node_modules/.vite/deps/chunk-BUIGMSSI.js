import {
  init_helperFunctions
} from "./chunk-5VPLAD3B.js";
import {
  ShaderStore,
  init_shaderStore
} from "./chunk-THV6IJPM.js";
import {
  __esm
} from "./chunk-V4OQ3NZ2.js";

// ../../../node_modules/@babylonjs/core/Shaders/copyTextureToTexture.fragment.js
var name, shader, copyTextureToTexturePixelShader;
var init_copyTextureToTexture_fragment = __esm({
  "../../../node_modules/@babylonjs/core/Shaders/copyTextureToTexture.fragment.js"() {
    init_shaderStore();
    init_helperFunctions();
    name = "copyTextureToTexturePixelShader";
    shader = `uniform float conversion;uniform sampler2D textureSampler;uniform float lodLevel;varying vec2 vUV;
#include<helperFunctions>
void main(void) 
{
#ifdef NO_SAMPLER
vec4 color=texelFetch(textureSampler,ivec2(gl_FragCoord.xy),0);
#else
vec4 color=textureLod(textureSampler,vUV,lodLevel);
#endif
#ifdef DEPTH_TEXTURE
gl_FragDepth=color.r;
#else
if (conversion==1.) {color=toLinearSpace(color);} else if (conversion==2.) {color=toGammaSpace(color);}
gl_FragColor=color;
#endif
}
`;
    if (!ShaderStore.ShadersStore[name]) {
      ShaderStore.ShadersStore[name] = shader;
    }
    copyTextureToTexturePixelShader = { name, shader };
  }
});

export {
  copyTextureToTexturePixelShader,
  init_copyTextureToTexture_fragment
};
//# sourceMappingURL=chunk-BUIGMSSI.js.map
